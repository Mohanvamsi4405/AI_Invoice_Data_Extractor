# Python 3.11 — Render native Python compatible

# ── Web Framework ──────────────────────────────────────────
fastapi
uvicorn[standard]
python-multipart
jinja2

# ── Text Extraction ─────────────────────────────────────────
pdfplumber          # PDF text extraction (pure Python, no system deps)
Pillow              # Image processing
pytesseract         # Image OCR (lightweight wrapper around Tesseract)
pdf2image           # Convert PDF pages to images if needed

# ── LangChain + Groq ────────────────────────────────────────
langchain
langchain-core
langchain-groq

# ── Excel ────────────────────────────────────────────────────
openpyxl

# ── Utilities ────────────────────────────────────────────────
python-dotenv
pydantic
httpx
aiofiles
